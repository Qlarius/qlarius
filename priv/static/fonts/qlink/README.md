# Qlink page fonts

Self-hosted SIL Open Font License (OFL) latin subsets (weights 400 and 700),
vendored from Fontsource packages. Public Qlink pages must not load Google Fonts
or any other font CDN.

Each `*-LICENSE.txt` is the upstream OFL text for that family.
