elixir_version=1.18.3
erlang_version=27.0

# Do not use hook_post_compile for assets — npm is not on PATH in the Elixir buildpack.
# JS/CSS are built by gigalixir-buildpack-phoenix-static (see .buildpacks + assets/package.json deploy).
