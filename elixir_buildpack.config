elixir_version=1.18.3
erlang_version=27.0

# Build and digest JS/CSS on every Gigalixir deploy (priv/static/assets is not in git).
# The buildpack runs $hook_post_compile as a single executable (no shell) — use bin/gigalixir_assets_deploy.sh.
hook_post_compile=bin/gigalixir_assets_deploy.sh
